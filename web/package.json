{
  "name": "local-galactic-structures-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Three.js MVP web viewer for the Local Galactic Structures scene export (spec Idea.md §22, §31; Story #64).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
