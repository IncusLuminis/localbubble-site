{
  "name": "localbubble-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Local Bubble - an interactive 3D map of the Solar neighborhood (Three.js viewer), published at localbubble.space.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
